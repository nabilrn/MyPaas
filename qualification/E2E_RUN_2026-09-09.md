# E2E qualification report — 2026-09-09

## Scope

Browser-based qualification of the deployed MyPaaS dashboard and public workload routes, plus a disposable database-backed Compose project lifecycle.

Checks against pre-existing projects and control-plane surfaces were observational/read-only. The disposable project portion was intentionally mutating: the project was created, deployed, verified through MyPaaS runtime state, and deleted after the lifecycle test. No pre-existing production project was modified.

## Evidence identity

This run did not capture enough environment identity to treat the report as commit-specific release evidence:

- exact deployed MyPaaS Git SHA: **not recorded during the run**;
- exact browser/version: **not recorded during the run**;
- host OS/runtime shape: **not recorded during the run**.

The report therefore records observed browser behavior on 2026-09-09 only. It must not be used to claim that a specific Git SHA, browser matrix, container runtime, or host configuration was qualified. Exact-SHA release qualification continues to come from CI and any controlled runtime checks that explicitly record their environment identity.

## Result

This is a stable-surface qualification report for the browser workflows that were actually exercised. It does not claim that optional VM, direct-API, external-webhook, credential-dependent, responsive, accessibility, cross-browser, or failure/recovery scenarios were executed.

The exercised workflows passed:

- authenticated dashboard access and refresh;
- owner navigation for Projects, Containers, Shell, and Administration;
- project detail navigation and operational pages;
- repository inspection and automatic Compose detection;
- environment-variable discovery with masked values;
- disposable project creation from a public PostgreSQL-backed Compose repository;
- Compose build, PostgreSQL readiness, service startup, route reconciliation, and live runtime state;
- deployment history and deployment-log inspection;
- project settings, webhook status, database detection, and danger-zone confirmation guard;
- Database Studio read-only connection/schema/table inspection and time-bounded write-mode enable/revoke;
- public Static and Compose workload page loads plus representative UI interactions;
- project deletion after explicit confirmation and dashboard removal verification;
- Administration General, Users, Update, Backup, Migration, MCP, Audit Logs, and Containers surfaces.

## Applicability and boundaries

The checklist uses **Passed**, **Failed**, and **Not Applicable/Blocked**. The following items were Not Applicable or Blocked for this run because their required fixture, execution channel, or dedicated browser coverage was unavailable:

### Integration / operational coverage not exercised

- direct `/api/health` and `/api/ready` requests (blocked by the browser client);
- GitHub OAuth logout/re-login and secondary-user authorization checks;
- webhook delivery from GitHub;
- Database Studio row editing was not applicable: the inspected project had no safe existing primary-key row, and the UI intentionally exposes no insert/delete operation;
- database inserts, deletes, and schema migrations are outside the Database Studio user-visible contract;
- backup creation/download and migration execution;
- secret reveal/edit/delete and credential regeneration;
- VM restart/reconciliation and system-update execution.

### Browser / accessibility / recovery coverage not exercised

- full responsive viewport coverage;
- keyboard-only navigation and accessibility qualification;
- cross-browser qualification;
- browser-observable failure/recovery matrices.

These are not failures of the exercised stable surface. Direct API, webhook, VM, database, backup/migration, and credential scenarios require their corresponding external channel or disposable fixture. Responsive, keyboard-only, cross-browser, and browser-observable failure/recovery scenarios require separate browser qualification.

## Stable qualification conclusion

All browser workflows actually exercised in this run passed. No pre-existing production project was modified, and the intentionally mutating disposable fixture was removed after verification.

Because the exact deployed SHA and environment identity were not recorded, this report is **supplemental product-surface evidence**, not release-SHA qualification. Remaining items must be reported separately under their actual browser, accessibility, recovery, operational, or integration qualification categories.
