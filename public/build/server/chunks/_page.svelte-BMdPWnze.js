import { c as store_get, f as unsubscribe_stores } from './renderer-EjaZHhrY.js';
import { p as page } from './stores-BdUEhWdH.js';
import { S as SectionPanel } from './SectionPanel-DrTEgZNe.js';
import './toast-4wUWO2xn.js';
import './root-d39-B9S_.js';
import './state.svelte-xyT85yFW.js';
import './index-CjGMQA9M.js';

function _page($$renderer, $$props) {
  $$renderer.component(($$renderer2) => {
    var $$store_subs;
    let writeAccess, writeExpiresAt, writeActive, columns;
    let tables = [];
    let tableSearch = "";
    let rowSearch = "";
    let appliedRowSearch = "";
    let enumFilters = {};
    let appliedEnumFilters = {};
    let now = Date.now();
    function cleanEnumFilters(filters) {
      return Object.fromEntries(Object.entries(filters).filter(([, value]) => Boolean(value)));
    }
    function formatDuration(ms) {
      const total = Math.max(0, Math.floor(ms / 1e3));
      const minutes = Math.floor(total / 60);
      const seconds = total % 60;
      return `${minutes}m ${seconds.toString().padStart(2, "0")}s`;
    }
    store_get($$store_subs ??= {}, "$page", page).params.id;
    writeAccess = null;
    writeExpiresAt = writeAccess ? new Date(writeAccess.expiresAt).getTime() : 0;
    writeActive = Boolean(writeAccess && writeExpiresAt > now);
    writeActive ? formatDuration(writeExpiresAt - now) : "";
    columns = [];
    columns.filter((column) => column.primaryKey);
    columns.filter((column) => (column.enumValues?.length ?? 0) > 0);
    columns.filter((column) => !column.autoGenerated && !column.primaryKey);
    columns.filter((column) => !column.autoGenerated);
    tables.filter((table) => table.name.toLowerCase().includes(tableSearch.trim().toLowerCase()));
    Number(Boolean(appliedRowSearch)) + Object.values(appliedEnumFilters).filter(Boolean).length;
    rowSearch.trim() !== appliedRowSearch || JSON.stringify(cleanEnumFilters(enumFilters)) !== JSON.stringify(appliedEnumFilters);
    let $$settled = true;
    let $$inner_renderer;
    function $$render_inner($$renderer3) {
      {
        $$renderer3.push("<!--[0-->");
        SectionPanel($$renderer3, {
          title: "Database Studio",
          description: "Loading database connection and schema metadata.",
          children: ($$renderer4) => {
            $$renderer4.push(`<div class="space-y-3"><div class="h-16 animate-pulse rounded-md bg-gray-100 dark:bg-gray-800"></div> <div class="h-48 animate-pulse rounded-md bg-gray-100 dark:bg-gray-800"></div></div>`);
          },
          $$slots: { default: true }
        });
      }
      $$renderer3.push(`<!--]--> `);
      {
        $$renderer3.push("<!--[-1-->");
      }
      $$renderer3.push(`<!--]-->`);
    }
    do {
      $$settled = true;
      $$inner_renderer = $$renderer2.copy();
      $$render_inner($$inner_renderer);
    } while (!$$settled);
    $$renderer2.subsume($$inner_renderer);
    if ($$store_subs) unsubscribe_stores($$store_subs);
  });
}

export { _page as default };
//# sourceMappingURL=_page.svelte-BMdPWnze.js.map
