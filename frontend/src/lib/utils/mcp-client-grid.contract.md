# MCP client identity contract

The Administration MCP page uses the existing local SVG symbol sprite at `/brands/agents/agent-logos.svg` and renders each supported client with its named brand color. The logos are identity marks only; MyPaaS layout, surfaces, strokes, and typography remain governed by `frontend/DESIGN.md`.
