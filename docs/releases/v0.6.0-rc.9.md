# MyPaaS v0.6.0-rc.9

## Included changes

- Aligns the Webhook Payload URL and Secret controls using a shared action column so credential rows remain visually consistent.
- Adds a GitHub platform mark to the Webhook page and GitHub setup section for clearer source context.
- Keeps webhook delivery, signature verification, secret rotation, and deployment behavior unchanged.

## Qualification focus

Install rc.9 from an rc.8 dashboard and confirm:
- the release badge/banner detects rc.9 automatically;
- the System Update progress flow still behaves correctly;
- the dashboard reconnects and reloads once after success;
- the Webhook page shows the GitHub mark;
- Payload URL and Secret rows remain aligned;
- reveal/copy/regenerate-secret actions still work as before.
